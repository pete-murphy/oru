{
  "name": "oru-frontend",
  "scripts": {
    "dev:tailwind": "tailwindcss -i ./input.css -o ./build/main.css --watch",
    "dev:elm": "elm-watch hot",
    "dev": "npm-run-all --parallel dev:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "elm-watch": "^1.2.0-beta.3",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.3.3"
  }
}
